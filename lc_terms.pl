% lc_terms.lp

lp_to_term(LP1,LP2) :-
	replace_current_predicates(LP,Term1),
	



replace_current_predicates(LP,Term1) :-

% stwa to find instances of cps, replace them and keep a list of corrs

sub_term_wa([[n,N], Term, Instances)

current_predicate(_,member(_,_)).
